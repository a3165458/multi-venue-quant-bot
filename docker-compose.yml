# ═══════════════════════════════════════════════════════
# Multi-Venue Quant Bot — Docker Compose
#
# 快速启动:
#   1. cp .env.example .env
#   2. 编辑 .env 中对应网络前缀的 API 凭证
#   3. docker compose up -d
#   4. 访问 http://localhost:3028 查看 Dashboard
# ═══════════════════════════════════════════════════════

services:
  multi-venue-quant-bot:
    build: .
    container_name: multi-venue-quant-bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "3028:3028"
    volumes:
      # PnL 数据持久化 (交易历史、净值曲线)
      - multi-venue-bot-data:/app/data
      # 可选: 挂载自定义配置
      # - ./config/settings.yaml:/app/config/settings.yaml:ro
    environment:
      - RUST_LOG=${RUST_LOG:-info}
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3028/api/status"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  multi-venue-bot-data:
    driver: local
